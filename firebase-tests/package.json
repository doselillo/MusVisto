{
  "name": "musvisto-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Tests de las reglas de seguridad RTDB de MusVisto contra el emulador de Firebase.",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only database --project mus-visto --config ../firebase.json \"node rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.13.0",
    "firebase-tools": "^13.23.1"
  }
}
